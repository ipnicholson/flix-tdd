<%= form_for([@movie, @review]) do |f| %>
  <%= render 'shared/errors' %>
  <fieldset>
    <ol>
      <li class="required">
        <%= f.label :name %><br/>
        <%= f.text_field :name, autofocus: true %>
      </li>
      <li class="required">
        <%= f.label :stars %><br/>
        <%= f.select :stars, Review::STARS, prompt: "Pick One" %>
      </li>
      <li class="required">
        <%= f.label :comment %><br/>
        <%= f.text_area :comment, cols: 40, rows: 3 %>
      </li>
    </ol>
    <p>
      <%= f.submit %> |
      <%= link_to "Cancel", movie_path(@movie), class: 'button' %>
    </p>
  </fieldset>
<% end %>